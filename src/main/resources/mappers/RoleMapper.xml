<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xxx.mapper.RoleMapper" >
  <resultMap id="BaseResultMap" type="com.xxx.bean.Role" >
    <id column="roleid" property="roleid" jdbcType="INTEGER" />
    <result column="rolename" property="rolename" jdbcType="VARCHAR" />
    <result column="roledesc" property="roledesc" jdbcType="VARCHAR" />
    <result column="available" property="available" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    roleid, rolename, roledesc, available,creattime,updatetime
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select
    <include refid="Base_Column_List" />
    from sys_role
    where roleid = #{roleid,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from sys_role
    where roleid = #{roleid,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.xxx.bean.Role" >
    insert into sys_role (roleid, rolename, roledesc, 
      available)
    values (#{roleid,jdbcType=INTEGER}, #{rolename,jdbcType=VARCHAR}, #{roledesc,jdbcType=VARCHAR}, 
      #{available,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.xxx.bean.Role" >
    insert into sys_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="roleid != null" >
        roleid,
      </if>
      <if test="rolename != null" >
        rolename,
      </if>
      <if test="roledesc != null" >
        roledesc,
      </if>
      <if test="available != null" >
        available,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="roleid != null" >
        #{roleid,jdbcType=INTEGER},
      </if>
      <if test="rolename != null" >
        #{rolename,jdbcType=VARCHAR},
      </if>
      <if test="roledesc != null" >
        #{roledesc,jdbcType=VARCHAR},
      </if>
      <if test="available != null" >
        #{available,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.xxx.bean.Role" >
    update sys_role
    <set >
      <if test="rolename != null" >
        rolename = #{rolename,jdbcType=VARCHAR},
      </if>
      <if test="roledesc != null" >
        roledesc = #{roledesc,jdbcType=VARCHAR},
      </if>
      <if test="available != null" >
        available = #{available,jdbcType=INTEGER},
      </if>
    </set>
    where roleid = #{roleid,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.xxx.bean.Role" >
    update sys_role
    set rolename = #{rolename,jdbcType=VARCHAR},
      roledesc = #{roledesc,jdbcType=VARCHAR},
      available = #{available,jdbcType=INTEGER}
    where roleid = #{roleid,jdbcType=INTEGER}
  </update>
<!--查询所有角色或用户id查询-->
  <select id="selectAllByRole" resultType="java.util.Map">
   SELECT
	r.rolename as rolename,
	r.roleid,
	CASE
	WHEN IFNULL(tp.userid,0)=0 THEN ""

	ELSE
		"selected"
    END as "selected"
    FROM
	sys_role AS r
	LEFT JOIN (
	SELECT
		u.userid,
		ru.rid
	FROM
		sys_user AS u
		LEFT JOIN sys_role_user AS ru ON u.userid = ru.uid
	WHERE
		u.available = 1
		AND u.userid =#{userid}
	) AS tp ON r.roleid = tp.rid
WHERE
	r.available =1

  </select>
  <!--参数条件查询角色-->
  <select id="selectAllByQuery" resultType="com.xxx.bean.Role">
    select <include refid="Base_Column_List"></include>
    from sys_role
    <where>
    available=1
      <if test="rolename!=null and rolename!=''">
        and rolename like "%${rolename}%"
      </if>
      <if test="roledesc!=null and roledesc!=''">
        and roledesc like "%${roledesc}%"
      </if>
      
    </where>
    
  </select>

</mapper>